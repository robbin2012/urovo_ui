# UROVO UI 设计规范

## 图标

- 所有图标统一使用 [Lucide Icons](https://lucide.dev/icons/) 图标库。
- 箭头也使用 Lucide 图标，不使用文字箭头或自绘箭头，箭头用 https://lucide.dev/icons/arrow-right
- 同一个箭头在正常状态和 Hover 状态下保持同一图形，只做位置、颜色或背景变化。

## 颜色

- 标题颜色：`#11141a`
- Description 和正文颜色：`#404f63`
- Description 使用正常颜色和不透明度，不需要额外变淡。

## 字重

- 所有标题统一使用字重 `500`。
- Description 使用正常字重，不要加粗。

## 链接

- “查看更多”“Explore”“View All”等小字统一使用 `14px`。

## 字号层级

- 全站统一使用当前项目字体 `Plus Jakarta Sans`。
- Hero 主标题：桌面端 `46–60px`，移动端 `32–36px`。
- 区块标题：桌面端 `40px`，移动端 `23–25px`。
- 普通卡片标题：`20–24px`。
- 重点卡片或行业大卡片标题：`28–32px`。
- 正文和 Description：桌面端 `16–18px`，移动端 `14–15px`。
- 导航和普通按钮：`14–16px`。
- “查看更多”“Explore”“View All”等辅助链接：`14px`。
- 当前字号层级整体合理；后续新增页面按以上范围使用，不要随意增加新的字号。
